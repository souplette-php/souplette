{#
    @see https://html.spec.whatwg.org/multipage/parsing.html#script-data-escape-start-state
#}
{% import 'tokenizer/_macros.php.twig' as h -%}

if ($cc === '-') {
    // Emit a U+002D HYPHEN-MINUS character token.
    $this->tokenQueue->enqueue(new Character('-'));
    // Switch to the script data escape start dash state.
    {{ h.advance_to('SCRIPT_DATA_ESCAPE_START_DASH') }}
} else {
    // Reconsume in the script data state.
    {{ h.reconsume_in('SCRIPT_DATA') }}
}
